

<% if current_customer.following?(customer) %>
  <%= link_to "フォロー中", customer_relationships_path(customer.id),class: "btn-follow text-primary font-weight-bold h4 mt-2",method: :delete, remote: true %>
<% else %>
     <%= link_to "フォロー", customer_relationships_path(customer.id),class: "btn-follow text-primary font-weight-bold h4 mt-2",method: :post, remote: true %>
<% end %>

