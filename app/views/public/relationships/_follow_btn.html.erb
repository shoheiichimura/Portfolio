

<% if current_customer.following?(customer) %>
  <%= link_to "フォロー中", customer_relationships_path(customer.id),class: "btn-follow text-primary font-weight-bold h4 mt-2 text-info",method: :delete, remote: true %>
<% else %>
     <%= link_to customer_relationships_path(customer.id),class: "btn-follow text-primary font-weight-bold h4 mt-2 text-body",method: :post, remote: true do %>
       フォローする<i class="far fa-thumbs-up fas-2x mx-1"></i>
     <% end %>
<% end %>

